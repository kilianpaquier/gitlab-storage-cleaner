# Code generated by craft; DO NOT EDIT.

version: 2

builds:
  - main: ./cmd/gitlab-storage-cleaner
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X github.com/kilianpaquier/gitlab-storage-cleaner/internal/build.branch={{ .Branch }}
      - -X github.com/kilianpaquier/gitlab-storage-cleaner/internal/build.commit={{ .Commit }}
      - -X github.com/kilianpaquier/gitlab-storage-cleaner/internal/build.date={{ .CommitDate }}
      - -X github.com/kilianpaquier/gitlab-storage-cleaner/internal/build.version={{ .Env.VERSION }}
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

announce:
  skip: true

changelog:
  disable: true

archives:
  - formats: tar.gz
    wrap_in_directory: false
    name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: zip

checksum:
  name_template: checksums.txt

nfpms:
  - maintainer: kilianpaquier
    license: MIT
    file_name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    formats:
      - apk
      - deb
      - rpm
