# Code generated by craft; DO NOT EDIT.

version: 1

builds:
  - main: cmd/gitlab-storage-cleaner/main.go
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X github.com/kilianpaquier/gitlab-storage-cleaner/internal/cobra.version={{ .Env.VERSION }}
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

announce:
  skip: true

changelog:
  skip: true

archives:
  - format: tar.gz
    wrap_in_directory: false
    name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt
  
nfpms:
  - maintainer: kilianpaquier
    license: MIT
    file_name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    formats:
      - apk
      - deb
      - rpm